name: device_meta_example
description: Demonstrates how to use Device meta.

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  device_meta:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true